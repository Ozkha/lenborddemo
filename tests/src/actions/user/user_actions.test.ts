import { describe, expect, it } from "vitest";

describe("User server actions", () => {
  it("create company admin", () => {
    expect(2 + 2).to.equal(4);
  });

  it("create no admin", () => {
    expect(2 + 2).to.equal(4);
  });

  it("update board responsabilities", () => {
    expect(2 + 2).to.equal(4);
  });

  it("update password", () => {
    expect(2 + 2).to.equal(4);
  });

  it("update role", () => {
    expect(2 + 2).to.equal(4);
  });

  it("update status", () => {
    expect(2 + 2).to.equal(4);
  });
});
